<!DOCTYPE html>
<html>
<head>
    <title>SARKARI JOB PORTAL | Govt Jobs 2026</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: Arial, sans-serif; 
            background: #f5f5f5; 
            color: #333; 
            line-height: 1.5;
        }
        
        /* Header */
        .header {
            background: #dc3545;
            color: white;
            padding: 15px 0;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: white;
            text-decoration: none;
        }
        
        /* Hero */
        .hero {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: white;
            text-align: center;
            padding: 60px 20px;
        }
        .hero h1 { font-size: 32px; margin-bottom: 15px; }
        .hero p { font-size: 18px; margin-bottom: 25px; }
        .search-box {
            max-width: 500px;
            margin: 0 auto;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        .search-input {
            flex: 1;
            padding: 15px;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            min-width: 250px;
        }
        .search-btn {
            padding: 15px 25px;
            background: #dc3545;
            color: white;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
        }
        
        /* Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Section Title */
        .section-title {
            text-align: center;
            font-size: 28px;
            margin: 40px 0 30px;
            color: #333;
        }
        
        /* Categories */
        .categories {
            background: white;
            padding: 40px 0;
        }
        .category-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }
        .category-card {
            text-align: center;
            padding: 25px 15px;
            background: #f8f9fa;
            border-radius: 15px;
            border-top: 4px solid #dc3545;
            cursor: pointer;
            transition: transform 0.3s;
        }
        .category-card:hover { transform: translateY(-5px); }
        .category-icon { font-size: 36px; display: block; margin-bottom: 12px; }
        .category-card h3 { font-size: 18px; margin-bottom: 8px; color: #333; }
        .category-card p { font-size: 14px; color: #666; }
        
        /* NEW 3-COLUMN SECTION */
        .three-column {
            padding: 40px 0;
            background: #f8f9fa;
        }
        .column-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 25px;
            margin-top: 30px;
        }
        .column {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            border-top: 5px solid #dc3545;
        }
        .column h3 {
            font-size: 22px;
            color: #dc3545;
            margin-bottom: 20px;
            text-align: center;
        }
        .item-card {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 15px;
            cursor: pointer;
            transition: all 0.3s;
            border-left: 4px solid #dc3545;
        }
        .item-card:hover {
            background: white;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transform: translateX(5px);
        }
        .item-title {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        .item-date {
            font-size: 14px;
            color: #666;
        }
        .details-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.8);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }
        .modal-content {
            background: white;
            max-width: 600px;
            width: 90%;
            max-height: 80vh;
            overflow-y: auto;
            border-radius: 15px;
            padding: 30px;
            position: relative;
        }
        .close-btn {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 30px;
            cursor: pointer;
            color: #666;
        }
        .close-btn:hover { color: #dc3545; }
        
        /* Jobs Section */
        .jobs {
            padding: 40px 0;
            background: white;
        }
        .jobs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 25px;
        }
        .job-card {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            border-left: 5px solid #dc3545;
        }
        .job-title { font-size: 20px; font-weight: bold; margin-bottom: 10px; }
        .job-meta { display: flex; gap: 15px; color: #666; margin-bottom: 15px; }
        .job-description { color: #555; }
        .apply-btn {
            width: 100%;
            padding: 12px;
            background: #dc3545;
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 15px;
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 20px;
            color: #666;
        }
        .empty-state h3 { font-size: 24px; color: #dc3545; margin-bottom: 15px; }
        
        /* Footer */
        .footer {
            background: #333;
            color: white;
            text-align: center;
            padding: 40px 20px;
        }
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        .footer-links a { color: #ccc; text-decoration: none; }
        
        /* RESPONSIVE */
        @media (max-width: 768px) {
            .logo { font-size: 20px; }
            .hero h1 { font-size: 24px; }
            .search-box { flex-direction: column; }
            .search-input { min-width: auto; }
            .section-title { font-size: 22px; }
            
            /* Mobile Categories - 2 columns */
            .category-grid { grid-template-columns: repeat(2, 1fr); gap: 12px; }
            .category-card { padding: 15px 10px; }
            .category-icon { font-size: 24px; margin-bottom: 8px; }
            .category-card h3 { font-size: 14px; }
            .category-card p { display: none; }
            
            /* Mobile 3-Column → 1 Column */
            .column-grid { grid-template-columns: 1fr; gap: 20px; }
            
            .jobs-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="header-content">
            <a href="/" class="logo">🔴 SARKARI JOB PORTAL</a>
        </div>
    </header>

    <!-- Hero -->
    <section class="hero">
        <h1>🔥 Latest Govt Jobs 2026</h1>
        <p>Delhi NCR • SSC • UPSC • Railway • Banking • Police</p>
        <div class="search-box">
            <input type="text" class="search-input" placeholder="🔍 Search Jobs/Results/Admit Card...">
            <button class="search-btn">Search</button>
        </div>
    </section>

    <!-- Categories -->
    <section class="categories">
        <div class="container">
            <h2 class="section-title">🏷️ Categories</h2>
            <div class="category-grid">
                <div class="category-card">
                    <span class="category-icon">📚</span>
                    <h3>SSC Jobs</h3>
                    <p>CGL CHSL MTS</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">👨‍🎓</span>
                    <h3>UPSC</h3>
                    <p>CSE CDS NDA</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">🚂</span>
                    <h3>Railway</h3>
                    <p>NTPC Group D</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">🏦</span>
                    <h3>Banking</h3>
                    <p>IBPS SBI PO</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">👮</span>
                    <h3>Police</h3>
                    <p>Delhi Police</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">👩‍🏫</span>
                    <h3>Teaching</h3>
                    <p>CTET TGT</p>
                </div>
            </div>
        </div>
    </section>

    <!-- NEW 3-COLUMN SECTION -->
    <section class="three-column">
        <div class="container">
            <h2 class="section-title">📊 Quick Updates</h2>
            <div class="column-grid">
                <!-- JOBS COLUMN -->
                <div class="column">
                    <h3>🔥 Latest Jobs</h3>
                    <% if (jobs && jobs.length > 0) { %>
                        <% jobs.slice(0, 4).forEach(job => { %>
                        <div class="item-card" onclick="showDetails('job', '<%= job._id %>')">
                            <div class="item-title"><%= job.title %></div>
                            <div class="item-date"><%= new Date(job.lastDate).toLocaleDateString('en-IN') %></div>
                        </div>
                        <% }) %>
                    <% } else { %>
                        <div class="item-card">No jobs yet</div>
                    <% } %>
                </div>

                <!-- RESULT COLUMN (Static Data - Admin add karega later) -->
                <div class="column">
                    <h3>📋 Results</h3>
                    <div class="item-card" onclick="showDetails('result', '1')">
                        <div class="item-title">SSC CGL Tier 1 Result 2026</div>
                        <div class="item-date">10 Jan 2026</div>
                    </div>
                    <div class="item-card" onclick="showDetails('result', '2')">
                        <div class="item-title">Delhi Police Constable Result</div>
                        <div class="item-date">15 Jan 2026</div>
                    </div>
                    <div class="item-card" onclick="showDetails('result', '3')">
                        <div class="item-title">Railway NTPC Result</div>
                        <div class="item-date">20 Jan 2026</div>
                    </div>
                </div>

                <!-- ADMIT CARD COLUMN -->
                <div class="column">
                    <h3>🎫 Admit Cards</h3>
                    <div class="item-card" onclick="showDetails('admit', '1')">
                        <div class="item-title">SSC CGL Tier 2 Admit Card</div>
                        <div class="item-date">25 Jan 2026</div>
                    </div>
                    <div class="item-card" onclick="showDetails('admit', '2')">
                        <div class="item-title">UPSC Prelims Admit Card</div>
                        <div class="item-date">05 Feb 2026</div>
                    </div>
                    <div class="item-card" onclick="showDetails('admit', '3')">
                        <div class="item-title">Delhi Police Admit Card</div>
                        <div class="item-date">12 Feb 2026</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Modal for Details -->
    <div id="detailsModal" class="details-modal" onclick="closeModal()">
        <div class="modal-content" onclick="event.stopPropagation()">
            <span class="close-btn" onclick="closeModal()">&times;</span>
            <div id="modalBody">
                <!-- Dynamic content here -->
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-links">
            <a href="#">About</a>
            <a href="#">Contact</a>
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
        </div>
        <p>© 2026 SARKARI JOB PORTAL • Delhi NCR Govt Jobs<br>
        ⚠️ Apply only on official websites. No fees required.</p>
    </footer>

    <script>
       <script>
    // Make jobs globally accessible
    window.jobsData = <%- JSON.stringify(jobs || []) %>;
    
    function showDetails(type, id) {
        let content = '';
        
        if (type === 'job') {
            const job = window.jobsData.find(j => j._id == id);
            if (job) {
                content = `
                    <h2 style="color: #dc3545; margin-bottom: 20px;">🔥 ${job.title}</h2>
                    <div style="background: #f8f9fa; padding: 20px; border-radius: 10px; margin: 20px 0;">
                        <p><strong>🏷️ Category:</strong> ${job.category}</p>
                        <p><strong>📅 Important Date:</strong> ${new Date(job.impDate).toLocaleDateString('en-IN')}</p>
                        <p><strong>📄 Description:</strong><br>${job.description || 'Latest government update'}</p>
                        
                        ${job.primaryLink ? `
                        <a href="${job.primaryLink}" target="_blank" class="apply-btn" style="margin: 15px 0 10px 0; display: inline-block;">
                            🚀 Visit Primary Link
                        </a>
                        ` : ''}
                        
                        ${job.secondaryLink ? `
                        <a href="${job.secondaryLink}" target="_blank" class="apply-btn" style="background: #28a745; margin-left: 10px; display: inline-block;">
                            📎 ${job.secondaryLinkName || 'Secondary Link'}
                        </a>
                        ` : ''}
                    </div>
                `;
            } else {
                content = '<h2>Job not found!</h2>';
            }
        }
        
        document.getElementById('modalBody').innerHTML = content;
        document.getElementById('detailsModal').style.display = 'flex';
    }

    function closeModal() {
        document.getElementById('detailsModal').style.display = 'none';
    }

    // Close modal when clicking outside
    document.getElementById('detailsModal').addEventListener('click', function(e) {
        if (e.target === this) closeModal();
    });

    // Search functionality
    document.querySelector('.search-input').addEventListener('keypress', function(e) {
        if (e.key === 'Enter') {
            alert('🔍 Search coming soon!');
            this.value = '';
        }
    });

    // Category clicks
    document.querySelectorAll('.category-card').forEach(card => {
        card.addEventListener('click', () => {
            alert('📂 Category filter coming soon!');
        });
    });
</script>

    </script>
</body>
</html>
