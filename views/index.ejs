<!DOCTYPE html>
<html>
<head>
    <title>SARKARI JOB PORTAL | Govt Jobs 2026</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: Arial, sans-serif; 
            background: #f5f5f5; 
            color: #333; 
            line-height: 1.5;
        }
        
        /* Header */
        .header {
            background: #dc3545;
            color: white;
            padding: 15px 0;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: white;
            text-decoration: none;
        }
        
        /* Hero */
        .hero {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: white;
            text-align: center;
            padding: 60px 20px;
        }
        .hero h1 {
            font-size: 32px;
            margin-bottom: 15px;
        }
        .hero p {
            font-size: 18px;
            margin-bottom: 25px;
        }
        .search-box {
            max-width: 500px;
            margin: 0 auto;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        .search-input {
            flex: 1;
            padding: 15px;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            min-width: 250px;
        }
        .search-btn {
            padding: 15px 25px;
            background: #dc3545;
            color: white;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
        }
        
        /* Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Section Title */
        .section-title {
            text-align: center;
            font-size: 28px;
            margin: 40px 0 30px;
            color: #333;
        }
        
        /* Categories - MOBILE 2 LINES */
        .categories {
            background: white;
            padding: 40px 0;
        }
        .category-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        .category-card {
            text-align: center;
            padding: 25px 15px;
            background: #f8f9fa;
            border-radius: 15px;
            border-top: 4px solid #dc3545;
            cursor: pointer;
            transition: transform 0.3s;
        }
        .category-card:hover {
            transform: translateY(-5px);
        }
        .category-icon {
            font-size: 36px;
            display: block;
            margin-bottom: 12px;
        }
        .category-card h3 {
            font-size: 18px;
            margin-bottom: 8px;
            color: #333;
        }
        .category-card p {
            font-size: 14px;
            color: #666;
        }
        
        /* Jobs */
        .jobs {
            padding: 40px 0;
            background: #f8f9fa;
        }
        .jobs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 25px;
        }
        .job-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            border-left: 5px solid #dc3545;
            transition: transform 0.3s;
        }
        .job-card:hover {
            transform: translateY(-5px);
        }
        .job-header {
            background: #dc3545;
            color: white;
            padding: 20px;
        }
        .job-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 8px;
        }
        .job-meta {
            display: flex;
            gap: 15px;
            font-size: 14px;
        }
        .job-body {
            padding: 20px;
        }
        .job-description {
            color: #666;
            margin-bottom: 20px;
        }
        .job-status {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            font-size: 14px;
        }
        .status-active {
            background: #d4edda;
            color: #155724;
        }
        .status-expired {
            background: #f8d7da;
            color: #721c24;
        }
        .apply-btn {
            width: 100%;
            padding: 15px;
            background: #dc3545;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            text-decoration: none;
            display: block;
            text-align: center;
            margin-top: 15px;
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 20px;
            color: #666;
        }
        .empty-state h3 {
            font-size: 24px;
            color: #dc3545;
            margin-bottom: 15px;
        }
        
        /* Footer */
        .footer {
            background: #333;
            color: white;
            text-align: center;
            padding: 40px 20px;
        }
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        .footer-links a {
            color: #ccc;
            text-decoration: none;
        }
        
        /* RESPONSIVE - MOBILE PERFECT */
        @media (max-width: 768px) {
            .logo { font-size: 20px; }
            .hero h1 { font-size: 24px; }
            .hero p { font-size: 16px; }
            .search-box { flex-direction: column; }
            .search-input { min-width: auto; }
            .section-title { font-size: 22px; }
            
            /* MOBILE CATEGORIES - 2 LINES */
            .category-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 12px;
            }
            .category-card {
                padding: 15px 10px;
            }
            .category-icon {
                font-size: 24px;
                margin-bottom: 8px;
            }
            .category-card h3 {
                font-size: 14px;
                margin-bottom: 4px;
            }
            .category-card p {
                display: none;
            }
            
            .jobs-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            .footer-links {
                gap: 20px;
            }
        }
        
        @media (max-width: 480px) {
            .container { padding: 0 15px; }
            .hero { padding: 40px 15px; }
            .category-grid { gap: 10px; }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="header-content">
            <a href="/" class="logo">🔴 SARKARI JOB PORTAL</a>
        </div>
    </header>

    <!-- Hero -->
    <section class="hero">
        <h1>🔥 Latest Govt Jobs 2026</h1>
        <p>Delhi NCR • SSC • UPSC • Railway • Banking • Police</p>
        <div class="search-box">
            <input type="text" class="search-input" placeholder="🔍 Search SSC CGL, UPSC, Railway NTPC...">
            <button class="search-btn">Find Jobs</button>
        </div>
    </section>

    <!-- Categories -->
    <section class="categories">
        <div class="container">
            <h2 class="section-title">🏷️ Job Categories</h2>
            <div class="category-grid">
                <div class="category-card">
                    <span class="category-icon">📚</span>
                    <h3>SSC Jobs</h3>
                    <p>CGL CHSL MTS</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">👨‍🎓</span>
                    <h3>UPSC</h3>
                    <p>CSE CDS NDA</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">🚂</span>
                    <h3>Railway</h3>
                    <p>NTPC Group D</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">🏦</span>
                    <h3>Banking</h3>
                    <p>IBPS SBI PO</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">👮</span>
                    <h3>Police</h3>
                    <p>Delhi Police</p>
                </div>
                <div class="category-card">
                    <span class="category-icon">👩‍🏫</span>
                    <h3>Teaching</h3>
                    <p>CTET TGT</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Jobs -->
    <section class="jobs">
        <div class="container">
            <h2 class="section-title">🔥 Latest Jobs</h2>
            
            <% if (jobs && jobs.length > 0) { %>
                <div class="jobs-grid">
                    <% jobs.slice(0, 12).forEach(job => { %>
                    <div class="job-card">
                        <div class="job-header">
                            <div class="job-title"><%= job.title %></div>
                            <div class="job-meta">
                                <span><%= job.category %></span>
                                <span>Delhi NCR</span>
                            </div>
                        </div>
                        <div class="job-body">
                            <p class="job-description">
                                <%= job.description || 'Latest government job. Apply now!' %>
                            </p>
                            <div class="job-status <%= new Date(job.lastDate) >= new Date() ? 'status-active' : 'status-expired' %>">
                                📅 <%= new Date(job.lastDate).toLocaleDateString('en-IN') %>
                            </div>
                            <a href="<%= job.link %>" target="_blank" class="apply-btn">🚀 Apply Now</a>
                        </div>
                    </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <h3>📝 Jobs Coming Soon!</h3>
                    <p>Latest Delhi NCR Govt Jobs will appear here.</p>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-links">
            <a href="#">About</a>
            <a href="#">Contact</a>
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
        </div>
        <p>© 2026 SARKARI JOB PORTAL • Delhi NCR Govt Jobs<br>
        ⚠️ Apply only on official websites. No fees required.</p>
    </footer>

    <script>
        // Search
        document.querySelector('.search-input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                alert('🔍 Search coming soon!');
                this.value = '';
            }
        });

        // Category click
        document.querySelectorAll('.category-card').forEach(card => {
            card.addEventListener('click', function() {
                alert('📂 Category filter coming soon!');
            });
        });
    </script>
</body>
</html>
